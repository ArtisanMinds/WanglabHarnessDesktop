export const PLUGIN_ID = 'dsh-tauri-model-config'

/** Rapid-MLX 的 OpenAI 兼容端点默认地址（`rapid-mlx serve` 的默认端口）。 */
export const RAPID_MLX_DEFAULT_BASE_URL = 'http://localhost:8000/v1'

/** 承载全部模型配置的 DSH 设置文档文件名（位于 `$DSH_HOME` 之下）。 */
export const SETTINGS_FILE_NAME = 'settings.yaml'
